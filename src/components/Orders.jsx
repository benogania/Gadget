import React, { useContext, useState } from "react";
import { CartData } from "../App";

const Orders = () => {
  

  return(
  <CartData.Provider>
  
  </CartData.Provider>
  )
};
export default Orders;
